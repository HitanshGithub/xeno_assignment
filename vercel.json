{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "nextjs",
  "installCommand": "rm -f package-lock.json && npm install",
  "buildCommand": "npm run db:generate && npm run build -w @cadence/web",
  "outputDirectory": "apps/web/.next"
}
